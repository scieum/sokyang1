# CLAUDE.md — S-Bus Link

> 속초시 중·고등학생 등·하교 편의를 위한 **실시간 초정밀 버스 정보 웹앱**.
> 이 문서는 Claude Code가 본 저장소에서 작업할 때 따르는 단일 기준 문서입니다.
> 작성/주석/커밋 메시지는 **한국어 위주**로 작성합니다. (코드 식별자는 영어, 설명은 한국어)

---

## 1. 제품 개요

- **프로젝트명:** S-Bus Link
- **비전:** 속초시 ITS 인프라를 학생들의 일상과 연결하여 버스 대기 시간을 최소화하는 스마트 등교 솔루션.
- **타겟 사용자:** 등교 시 1번·7번·9번 등 주요 노선을 이용하며 도착 불확실성에 불안을 느끼는 속초시 중·고교생.
- **핵심 가치:** "버스가 나를 향해 움직이는 것"을 실시간으로 보여줘 심리적 안정감 제공.
- **성공 지표(SMART):**
  - 등·하교 시간대 평균 대기 시간 30% 감소
  - 실시간 차량 트래킹 표시 지연 최소화 (서버 데이터 1초 주기, 화면 보간 60fps)
  - 에러율 0.1% 이하, 초기 로딩 3초 이내

---

## 2. 기술 스택 (인터뷰로 확정)

| 영역 | 선택 | 비고 |
| :--- | :--- | :--- |
| 프론트엔드 | **Vite + React + TypeScript** | SSR 불필요, 구현 간편성 우선 |
| PWA | **vite-plugin-pwa** (Workbox) | 무설치, QR 접속, 오프라인 셸 |
| 지도/시각화 | **Leaflet + OpenStreetMap** | 무료, API 키 불필요 |
| 백엔드 | **Node.js + Fastify (TypeScript)** | 풀링 데몬 + API 서버 |
| 캐시 | **Redis** | 실시간 위치/도착 정보 인메모리 적재 |
| 실시간 전송 | **WebSocket (`ws`)** | 양방향 푸시, 수동 새로고침 불필요 |
| 외부 데이터 | **공공데이터포털 국토교통부 TAGO API** | 노선/정류소/도착/위치 정보 |

> **확정 사유 메모**
> - 프론트엔드: "가장 구현 간편한 것" 요청 → Next.js의 SSR 복잡도 없이 Vite SPA가 PWA 설정이 가장 단순.
> - 백엔드: Supabase 대신 직접 제어 폭이 넓은 Node 자체 백엔드 + WebSocket 선택.

---

## 3. 시스템 아키텍처

```
[공공데이터포털 TAGO API]
        │ (2~3초 간격 선제 수집)
        ▼
[풀링 데몬 (Node/Fastify)] ──► [Redis 캐시] ──► [API 서버] ──► [WebSocket 허브]
                                                                    │ (push)
                                                                    ▼
                                                  [PWA 클라이언트 (Vite+React)]
                                                          │
                                                  [Leaflet 지도 + 60fps 마커 보간]
```

1. **풀링 데몬:** 외부 API를 2~3초 간격으로 선제 수집해 외부 호출 부하·쓰로틀링을 차단.
2. **Redis:** 차량 위치/도착 정보를 해시 형태로 적재해 응답 속도 극대화.
3. **WebSocket 허브:** 클라이언트 구독(노선/정류소 단위)에 맞춰 변경분만 푸시.
4. **GTFS Realtime 호환:** 향후 글로벌·구글 지도 연동 대비 데이터 모델을 GTFS-RT 규격에 맞춰 설계.

---

## 4. 디렉터리 구조 (목표)

```
sokyang1/
├─ apps/
│  ├─ web/            # Vite + React + TS PWA (프론트엔드)
│  └─ server/         # Fastify + Redis + ws (백엔드/데몬)
├─ packages/
│  └─ shared/         # 타입 정의, GTFS-RT 모델, 상수 (프론트/백 공용)
├─ CLAUDE.md
└─ README.md
```

> 모노레포(npm/pnpm workspaces) 기준. 단일 패키지로 단순화 가능하면 추후 조정.

---

## 5. 외부 API (TAGO) 사용 규칙

| 용도 | API | 핵심 데이터 |
| :--- | :--- | :--- |
| 노선 마스터 | 버스노선정보 | 노선 ID, 노선번호, 기·종점 |
| 정류소 위치 | 버스정류소정보 | 위·경도(WGS84) → 지도 마커 |
| 도착 예정 | 버스도착정보 | 도착 예상 시간(초), 남은 정류장 수 |
| 초정밀 위치 | 버스위치정보 | 차량 실시간 GPS 좌표 |
| 광역 연계 | 시외버스정보 | 터미널 발 광역 노선 스케줄 |

- **API 키는 절대 커밋 금지.** `.env` 사용, `.env.example`만 저장소에 포함.
- 외부 API 직접 호출은 **백엔드 데몬에서만** 수행. 프론트엔드는 자체 백엔드만 호출.
- 표준 ID 체계: `권역(3) + 수단(1) + 일련번호(5)` 9자리 국가 표준 준수.
- 좌표계는 WGS84로 통일.

---

## 6. 프론트엔드 가이드

- **마커 보간:** 서버 데이터가 1초 주기로 들어와도 `requestAnimationFrame` 루프로 60fps 부드럽게 이동.
- **인지 심리 반영:** "N분 뒤 도착" 텍스트보다 버스가 다가오는 시각 정보를 우선 노출.
- **PWA:** QR 스캔 → 무설치 즉시 접속. 오프라인 셸/아이콘/매니페스트 구성.
- **상태 관리:** 우선 React 내장(`useState`/`useReducer`/Context)으로 시작, 필요 시 경량 라이브러리(zustand) 도입.
- **접근성:** 학생 대상 모바일 우선, 큰 터치 타깃, 색약 고려 색상.

---

## 7. 보안 / 프로덕션 체크리스트

- **인증/세션:** OWASP ASVS 기준. 세션 쿠키에 `HttpOnly`, `Secure`, `SameSite` 지정.
- **비밀값:** API 키·토큰은 환경변수로만 관리, 저장소·로그에 노출 금지.
- **입력 검증:** 모든 외부/클라이언트 입력 스키마 검증(예: zod).
- **레이트 리밋:** 자체 API에 요청 제한 적용.
- **모니터링:** 에러율 0.1% 이하 목표, 실시간 관제/로깅 도입.

---

## 8. 개발 워크플로 / 규칙 (Claude 준수 사항)

- **언어:** 문서·주석·커밋 메시지·PR 설명은 **한국어 위주**. 코드 식별자(변수/함수/타입)는 영어.
- **브랜치:** `claude/vigilant-thompson-03qdy` 에서 개발. 지시 없이 다른 브랜치로 push 금지.
- **커밋:** 작고 의미 있는 단위로, 한국어 설명 메시지. 사용자가 요청할 때만 push.
- **PR:** 사용자가 명시적으로 요청할 때만 생성.
- **비밀값:** `.env`는 커밋하지 않음, `.env.example` 유지.
- **타입:** TypeScript `strict` 모드 사용.
- **검증:** 변경 후 빌드/린트/테스트를 실행하고 결과를 사실대로 보고.

### 자주 쓰는 명령 (스캐폴딩 후 채워질 예정)

```bash
# 프론트엔드
cd apps/web && npm run dev        # 개발 서버
cd apps/web && npm run build      # 프로덕션 빌드

# 백엔드
cd apps/server && npm run dev     # 데몬 + API 서버
```

---

## 9. 다음 단계 (로드맵)

1. 모노레포 + Vite/React PWA + Fastify 서버 스캐폴딩
2. TAGO API 연동(노선·정류소 마스터 수집) 및 `.env.example` 작성
3. 풀링 데몬 + Redis 캐시 구축
4. WebSocket 푸시 파이프라인
5. Leaflet 지도 + 60fps 마커 보간 렌더링
6. PWA 설정(매니페스트/서비스워커/QR)
7. 보안·모니터링·성능 튜닝

> 이 로드맵은 사용자 승인 후 단계별로 진행한다.
